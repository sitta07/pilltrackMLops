name: PillTrack MLOps Pipeline 💊

on:
  push:
    tags:
      - 'v*'
      - 'pill-*'
      - 'box-*'
      - 'major-pill-*'
      - 'major-box-*'

permissions:
  contents: write

jobs:
  train-and-deploy:
    name: Smart Training Pipeline
    runs-on: self-hosted
    env:
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Global Environment Variables
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV

      # ✅ [Universal Path]
      - name: 🔗 Link Local Dataset to Runner (Universal)
        run: |
          TARGET_DATA_PATH="$HOME/Desktop/pilltrackMLops/data"
          echo "🔍 Auto-detecting dataset at: $TARGET_DATA_PATH"
          mkdir -p data
          
          if [ -d "$TARGET_DATA_PATH/raw_box" ]; then
             ln -sfn "$TARGET_DATA_PATH/raw_box" ./data/raw_box
             echo "✅ Box Data Linked"
          fi
          
          if [ -d "$TARGET_DATA_PATH/raw_pill" ]; then
            ln -sfn "$TARGET_DATA_PATH/raw_pill" ./data/raw_pill
            echo "✅ Pill Data Linked (raw_pill)"
          elif [ -d "$TARGET_DATA_PATH/raw" ]; then
            ln -sfn "$TARGET_DATA_PATH/raw" ./data/raw_pill
            echo "✅ Pill Data Linked (raw)"
          else
            echo "❌ ERROR: Pill dataset not found!"
            exit 1
          fi

      - name: Setup Venv
        run: |
          if [ ! -d "venv" ]; then /opt/homebrew/bin/python3.10 -m venv venv; fi
          echo "$PWD/venv/bin" >> $GITHUB_PATH

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install Pillow transparent-background
          pip install -r requirements.txt
          pip install "dvc[s3]>=3.0" python-dotenv

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: DVC Pull Models (Global)
        run: |
          dvc remote modify myremote --local access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          dvc remote modify myremote --local secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          # ดึงแบบกว้างๆ ไปก่อน
          dvc pull experiments/ metrics/ --force || echo "⚠️ No models on S3 yet."

      # ✅ [Fixed Logic] เพิ่มการดึง Base Model ก่อน Commit
      - name: Run Pipeline
        run: |
          echo "🏷️ Processing Tag: $TAG_NAME"
          
          # --- BOX Workflow ---
          if [[ "$TAG_NAME" =~ "box" ]]; then
            if [[ "$TAG_NAME" =~ "major" ]]; then
              echo "🔴 Major Box: Full Train"
              dvc repro train_box -f
            else
              echo "🟢 Incremental Box: Finetune Only"
              
              # 1. ต้องดึง Base Model มาก่อน ไม่งั้น Commit ไม่ผ่าน! [cite: 2026-01-29]
              echo "📥 Fetching Base Box Model..."
              dvc pull experiments/arcface_lite_v1/box --force || echo "⚠️ Base model not found on Cloud!"
              
              dvc repro augment_box
              dvc commit train_box -f 
              dvc repro evaluate_box
            fi
          fi
          
          # --- PILL Workflow ---
          if [[ "$TAG_NAME" =~ "pill" ]]; then
            if [[ "$TAG_NAME" =~ "major" ]]; then
              echo "🔴 Major Pill: Full Train"
              dvc repro train_pill -f
            else
              echo "🟢 Incremental Pill: Finetune Only"
              
              # 1. ต้องดึง Base Model มาก่อน ไม่งั้น Commit ไม่ผ่าน! [cite: 2026-01-29]
              echo "📥 Fetching Base Pill Model..."
              dvc pull experiments/arcface_lite_v1/pill --force || echo "⚠️ Base model not found on Cloud!"
              
              dvc repro augment_pill
              dvc commit train_pill -f
              
              dvc repro finetune_pill
              dvc repro evaluate_pill
            fi
          fi

      - name: Push Artifacts to DVC
        run: dvc push

      - name: 🆙 Update Lockfile to GitHub
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add dvc.lock
          if git diff --staged --quiet; then
            echo "No changes in dvc.lock"
          else
            git commit -m "🆙 update dvc.lock after training [skip ci]"
            git push origin HEAD:main
          fi

      - name: Deploy to S3 Releases
        run: |
          CLEAN_VER=$(echo $TAG_NAME | sed 's/major-//' | sed 's/pill-//' | sed 's/box-//')
          if [[ "$TAG_NAME" =~ "pill" ]]; then MODEL_TYPE="pill"; else MODEL_TYPE="box"; fi
          
          if [[ "$TAG_NAME" =~ "major" ]]; then
             PATH_TYPE="arcface_lite_v1"
          else
             PATH_TYPE="arcface_finetuned"
          fi
          
          python src/deploy.py \
            --version "$CLEAN_VER" \
            --path "experiments/$PATH_TYPE" \
            --type "$MODEL_TYPE" \
            --bucket "${{ secrets.S3_BUCKET_NAME }}" \
            --note "Auto-deployed for $TAG_NAME"

      - name: ⚡️ Sync Models to Local Dev Workspace
        run: |
          DEV_WORKSPACE="$HOME/Desktop/pilltrackMLops"
          echo "🚀 Teleporting models to: $DEV_WORKSPACE"
          if [ -d "$DEV_WORKSPACE" ]; then
            cp -R experiments/ "$DEV_WORKSPACE/experiments/"
            cp -R metrics/ "$DEV_WORKSPACE/metrics/" || true
            echo "✅ Sync Complete!"
          fi