name: PillTrack MLOps Pipeline 💊

on:
  push:
    tags:
      - 'v*'
      - 'pill-*'
      - 'box-*'
      - 'major-pill-*'
      - 'major-box-*'

jobs:
  train-and-deploy:
    name: Smart Training Pipeline
    runs-on: self-hosted
    env:
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      # ✅ [Senior Fix 1] บันทึก Tag Name เข้า Environment กลางให้ทุก Step มองเห็น [cite: 2026-01-29]
      - name: Set Global Environment Variables
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          echo "TAG_NAME=$TAG" >> $GITHUB_ENV
          echo "🏷️ Global Tag Set: $TAG"

      # ✅ [Senior Link] เชื่อมท่อข้อมูลจากเครื่องหลักเข้า Runner Workspace
      - name: 🔗 Link Local Dataset to Runner
        run: |
          echo "🔗 Linking local images..."
          mkdir -p data
          ln -sfn /Users/sittasahathum/Desktop/pilltrackMLops/data/raw_box ./data/raw_box
          ln -sfn /Users/sittasahathum/Desktop/pilltrackMLops/data/raw_pill ./data/raw_pill
          echo "✅ Dataset linked!"

      - name: Setup Venv
        run: |
          if [ ! -d "venv" ]; then /opt/homebrew/bin/python3.10 -m venv venv; fi
          echo "$PWD/venv/bin" >> $GITHUB_PATH

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install Pillow transparent-background
          pip install -r requirements.txt
          pip install "dvc[s3]>=3.0" python-dotenv

      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: DVC Pull Models
        run: |
          dvc remote modify myremote --local access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          dvc remote modify myremote --local secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          echo "📥 Pulling models from S3..."
          dvc pull experiments/ metrics/ --force || echo "⚠️ No models on S3 yet."

      # ✅ [Senior Logic] รันตาม Tag Name ที่ดึงจาก $TAG_NAME [cite: 2025-12-05]
      - name: Run Pipeline
        run: |
          echo "🏷️ Processing Tag: $TAG_NAME"

          if [[ "$TAG_NAME" =~ "box" ]]; then
            if [[ "$TAG_NAME" =~ "major" ]]; then
              echo "🔴 Mode: BOX Major Update"
              dvc repro train_box -f
            else
              echo "🟢 Mode: BOX Incremental Update"
              dvc repro augment_box && dvc commit train_box && dvc repro evaluate_box
            fi
          fi

          if [[ "$TAG_NAME" =~ "pill" ]]; then
            if [[ "$TAG_NAME" =~ "major" ]]; then
              echo "🔴 Mode: PILL Major Update"
              dvc repro train_pill -f
            else
              echo "🟢 Mode: PILL Incremental Update"
              dvc repro augment_pill && dvc commit train_pill && dvc repro evaluate_pill
            fi
          fi

      - name: Push Artifacts
        run: |
          echo "☁️ Uploading Models to S3..."
          dvc push

      # ✅ [Senior Fix 2] ใช้ตัวแปรกลาง และระบุ --bucket ให้ครบถ้วน [cite: 2026-01-29]
      - name: Deploy to S3 Releases
        run: |
          # ดึงเวอร์ชันออกมาจาก Tag
          CLEAN_VER=$(echo $TAG_NAME | sed 's/major-//' | sed 's/pill-//' | sed 's/box-//')
          
          # ตัดสินใจ Path ตามประเภท Tag
          if [[ "$TAG_NAME" =~ "major" ]]; then 
            PATH_TYPE="arcface_lite_v1"
          else 
            PATH_TYPE="arcface_finetuned"
          fi

          # ระบุประเภทโมเดล
          if [[ "$TAG_NAME" =~ "pill" ]]; then 
            MODEL_TYPE="pill"
          else 
            MODEL_TYPE="box"
          fi
          
          echo "🚀 Deploying $MODEL_TYPE model version $CLEAN_VER from $PATH_TYPE..."
          python src/deploy.py \
            --version "$CLEAN_VER" \
            --path "experiments/$PATH_TYPE" \
            --type "$MODEL_TYPE" \
            --bucket "${{ secrets.S3_BUCKET_NAME }}" \
            --note "Auto-deployed for $TAG_NAME via GitHub Actions"