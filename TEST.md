. 🛑 กรณีเทรนใหม่หมด (Major Update)
สถานการณ์: เมื่ออยากล้างไพ่, เปลี่ยนโครงสร้างโมเดล, หรือ Data เปลี่ยนเยอะมากจน Finetune เอาไม่อยู่ สิ่งที่ระบบทำ: เทรน Base Model ใหม่ตั้งแต่ต้น (รัน train_pill จริงๆ)

💊 สำหรับ Pill (ยาเม็ด)
Bash

# 1. ยิง Tag ที่มีคำว่า "major-pill"
git tag major-pill-test-v1.1

# 2. ส่งคำสั่ง
git push origin major-pill-test-v1.1
📦 สำหรับ Box (กล่องยา)
Bash

# 1. ยิง Tag ที่มีคำว่า "major-box"l
git tag major-box-v1.0

# 2. ส่งคำสั่ง
git push origin major-box-v1.0


2. 🟢 กรณีเพิ่มยาตัวใหม่ (Incremental / Add Class) (ใช้บ่อยสุด)
สถานการณ์: มีรูปยาตัวใหม่มาเพิ่ม (เช่น Panadol) อยากให้โมเดลรู้จักเพิ่มโดยไม่ต้องรอเทรนนาน สิ่งที่ระบบทำ:

ข้ามการเทรน Base Model (ใช้ dvc commit -f)

ดึง Base Model เก่าลงมา

Finetune เฉพาะส่วนหัว

Auto-Promote: เอาตัวใหม่ไปทับ Base Model เก่าทันที (เพื่อให้จำถาวร)

💊 สำหรับ Pill (ยาเม็ด)
Bash

# 1. ยิง Tag ที่ขึ้นต้นด้วย "pill-" (ห้ามมีคำว่า major)
git tag pill-v1.1

# 2. ส่งคำสั่ง
git push origin pill-v1.1
📦 สำหรับ Box (กล่องยา)
Bash

# 1. ยิง Tag ที่ขึ้นต้นด้วย "box-" (ห้ามมีคำว่า major)
git tag box-v1.1

# 2. ส่งคำสั่ง
git push origin box-v1.1